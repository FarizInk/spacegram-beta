
# Spacegram

Private Storage

1. Copy env file
   ```cp .env.example .env```
2. Setting your own env
3. Build frontend (optional)
   ```cd frontend && npm install && npm run build && cd ../```
4. Run it
   ```deno task serve```
